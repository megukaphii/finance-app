services:
  financeapp.server:
    image: financeapp.server
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - type: bind
        source: ../certificate.pfx
        target: /certificate.pfx
        read_only: true
volumes:
  cert: