<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:FinanceApp.Data.Models;assembly=FinanceApp.Data"
             xmlns:viewmodel="clr-namespace:FinanceApp.MauiClient.ViewModel"
             x:DataType="viewmodel:CounterpartiesViewModel"
             x:Class="FinanceApp.MauiClient.View.Counterparties"
             Title="{Binding Title}">

	<VerticalStackLayout>
		<!--<Entry TextChanged="CounterpartyName_OnTextChanged" Text="{Binding Search}" Placeholder="Counterparty..." />
		<CollectionView
			ItemsSource="{Binding CounterpartiesSearch}"
			SelectionChangedCommand="{Binding SelectCounterpartyCommand}"
			SelectionChangedCommandParameter="{Binding Path=SelectedItem, Source={RelativeSource Self}}"
			SelectionMode="Single">
			<CollectionView.ItemsLayout>
				<LinearItemsLayout Orientation="Vertical" />
			</CollectionView.ItemsLayout>
			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="models:Counterparty">
					<Border HeightRequest="50" Padding="10,0" BackgroundColor="Black">
						<Label Text="{Binding Name}" TextColor="White" VerticalTextAlignment="Center" />
					</Border>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>-->
	</VerticalStackLayout>

</ContentPage>